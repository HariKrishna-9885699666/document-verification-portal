# =============================================================================
# Frontend (Next.js) - Standalone Development Environment
# =============================================================================
# Used when running the frontend directly (not via Docker Compose):
#   cd frontend && npm run dev
# =============================================================================

# Public API URL exposed to the browser
NEXT_PUBLIC_API_URL=http://localhost:4000/api
