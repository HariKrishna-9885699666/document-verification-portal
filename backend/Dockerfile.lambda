FROM node:22-alpine

WORKDIR /app

RUN apk add --no-cache curl

COPY package.json package-lock.json* ./
RUN npm ci --omit=dev

COPY . .
RUN npm run build

EXPOSE 4000

# Standard NestJS HTTP server (for local dev)
# In production this runs as Lambda via serverless.yml
CMD ["node", "dist/main"]
